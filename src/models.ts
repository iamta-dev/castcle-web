export interface PhoneCountryCode {
    country: string;
    code: string;
}

export interface Telephone {
    country: string;
    code: string;
    phoneNumber: string;
}

export interface DropdownOption {
    label: string,
    value: any
}